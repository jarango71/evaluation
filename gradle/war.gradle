apply plugin: "war"

bootWar {
    mainClassName = "cl.nisum.evaluation.EvaluationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
